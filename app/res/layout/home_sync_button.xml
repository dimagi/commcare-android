<?xml version="1.0" encoding="utf-8"?>
<org.commcare.android.view.SquareButtonWithNotification
    android:id="@+id/home.sync.sqbn"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_weight="1"
    app:subtitle="Sync with server"
    app:backgroundColor="@color/cc_brand_color"
    app:img="@drawable/home_sync"
    app:notificationBackgroundColor="@color/cc_brand_text"
    app:textColor="@color/white"
    app:notificationTextColor="@color/white"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"/>
