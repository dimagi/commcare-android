<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/connect_view_info_card_border"
    android:padding="@dimen/standard_spacer_double"
    tools:theme="@style/ConnectTheme">

    <TextView
        android:id="@+id/info_card_value_text"
        style="@style/TextStyle.Connect.TitleXl"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:includeFontPadding="false"
        android:textColor="?attr/connectOnSurfaceVariant"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="40" />

    <LinearLayout
        android:id="@+id/info_card_text_container"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/standard_spacer_double"
        android:layout_marginEnd="@dimen/connect_space_sm"
        android:orientation="vertical"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@id/info_card_arrow"
        app:layout_constraintStart_toEndOf="@id/info_card_value_text"
        app:layout_constraintTop_toTopOf="parent">

        <TextView
            android:id="@+id/info_card_title_text"
            style="@style/TextStyle.Connect.BodyM"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:includeFontPadding="false"
            android:textColor="?attr/colorOnSurface"
            tools:text="Maximum Visits" />

        <TextView
            android:id="@+id/info_card_subtitle_text"
            style="@style/TextStyle.Connect.LabelS"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/connect_space_xs"
            android:textColor="?attr/connectOutline"
            android:visibility="gone"
            tools:text="5 visits per day"
            tools:visibility="visible" />

    </LinearLayout>

    <ImageView
        android:id="@+id/info_card_arrow"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="@null"
        android:src="@drawable/ic_chevron_right"
        android:tint="?attr/connectOutline"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:visibility="visible" />

</androidx.constraintlayout.widget.ConstraintLayout>
